## Tasks

- Create an exact replica of this repository manually.
- Implement authentication middleware (`isAuthenticated`).
- Add access token and refresh token generation.
- Build refresh token regeneration API.
- Create the `/me` route for current user details.
- Implement role-based access control middleware.
